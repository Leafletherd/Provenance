---
id: PR-13
target: Provenance
status: shipped
date_drafted: 2026-05-14
date_shipped: 2026-05-14
depends_on: [PR-12]
supersedes: []
summary: HARD FIX — re-land the PR-12 items that didn't actually ship: pasteboard toggle gone from Overview (Settings only); "X days ago" gone from sidebar; Home placeholder text matches spec; project-aware prompt confirmed working. Verification by visual inspection.
---

# PR-13 — Re-land PR-12's missed items

PR-12 marked shipped but user testing 2026-05-14 confirms several items did not actually land. This prompt forces them to land with mandatory visual verification.

**For every item below, the verification protocol requires you to:**
1. Build and launch Provenance
2. Navigate to the surface mentioned
3. Confirm the change with your eyes (not just the code)
4. If it's not as specified — keep working until it is

---

## Issue 1 — Pasteboard toggle still appears in Overview tab

**Verified by user:** the per-project "Track pastes in this project" toggle is STILL on the Overview tab. PR-12 §1 explicitly required removing it.

### What must be true

- Settings → Privacy section has the **only** "Track paste sources" toggle (global setting).
- The Overview tab has **no** pasteboard-related toggle anywhere.
- Search the codebase for any reference to per-project paste tracking UI and remove it.

### Verification

1. Launch Provenance. Open Settings (⌘,). Confirm the Privacy section has the global "Track paste sources" toggle.
2. Open any project. Click the Overview tab. Scroll the entire tab. Confirm there is NO pasteboard toggle anywhere on the Overview surface.
3. If you find one — remove it. Then verify again.

---

## Issue 2 — "X days ago" / relative time STILL in sidebar

**Verified by user:** screenshot 2026-05-14 shows sidebar project rows displaying "4 hr, 23 min" / "3 hr, 28 min" relative timestamps. PR-12 §3 explicitly required removing these.

### What must be true

- Each sidebar project row shows ONLY: status dot, project display name, folder path subtitle.
- **No relative time** ("X days ago", "4 hr, 23 min", "yesterday", etc.) anywhere in the sidebar row.
- Visual layout cleaner — fewer lines per row.

### Verification

1. Launch Provenance. Connect at least one project (or open with existing projects).
2. Look at the sidebar.
3. Each row should be: a small colored status dot, the project display name (15pt medium), and the folder path subtitle (11pt muted) BELOW the name. Nothing else.
4. If you see "4 hr, 23 min" or anything like it — find that label rendering in `SidebarView.swift` and **delete the line**.
5. Verify by inspection.

---

## Issue 3 — Home view placeholder text doesn't match spec

**Verified by user observation:** PR-12 §3 specified the placeholder text in the check-in field should read:

> "Ready to check in? Type a sentence or two."

Current placeholder appears to be different (something like "What did you work on? How does it feel?"). Replace it.

### What must be true

- The check-in TextEditor's placeholder reads exactly: `Ready to check in? Type a sentence or two.`
- (Not "what did you work on" or any other variation.)
- Italic styling is acceptable but not required.

### Verification

1. Launch Provenance. Go to Home view.
2. Look at the check-in input field's placeholder text.
3. It must read: `Ready to check in? Type a sentence or two.`
4. If different — change it.

---

## Issue 4 — Project-aware prompt updates (verify working)

**Status uncertain:** PR-12 §3 required that clicking a project card in the strip below updates the prompt above to reference that project. Verify this is working.

### What must be true

- Home view has the project cards strip below the prompt block.
- Clicking a project card → the prompt above updates to "How did your last session on `<that project's display name>` go?" (or equivalent project-named prompt).
- Clicking the check-in Save button writes the check-in to whichever project the prompt currently names.

### Verification

1. Launch Provenance with at least 2 connected projects.
2. Go to Home view.
3. Note which project the prompt names.
4. Click a different project card in the strip.
5. The prompt above should update to that new project's name.
6. Type a check-in and save. Open the project named in the prompt. Confirm the check-in landed there.
7. If any of those steps fail — fix the binding so prompt updates and save target the clicked project.

---

## Issue 5 — Centered prompt block (verify)

**Status uncertain:** PR-12 §3 required the prompt + text input + status picker + save button to be horizontally centered (max-width ~560pt) on the Home view.

### What must be true

- The prompt block is horizontally centered in the Home view's content area.
- Max-width on the block is ~560pt.
- The greeting line at the top stays left-aligned at `Brand.spaceLG`.
- On wide windows the prompt block does NOT stretch the full width — it stays centered with empty space on either side.

### Verification

1. Launch Provenance with the window resized to ~1500pt wide.
2. Go to Home view.
3. The "Good evening." greeting should be left-aligned near the top.
4. Below it, the prompt + check-in field + status picker + save button should form a block that is horizontally CENTERED in the available width, with visible empty space to the left and right.
5. The block should NOT stretch to the full window width.
6. If it does — apply `.frame(maxWidth: 560).frame(maxWidth: .infinity)` (or equivalent) to center it.

---

## Issue 6 — Audit other PR-12 items

While you're in the codebase, verify the rest of PR-12 actually landed:
- "+ New Project" button in the unified toolbar (top right or wherever it lives)
- Sidebar context menu still has Promote to Works / Reveal in Finder / etc.

If any of these aren't visible/working, fix them too.

---

## Acceptance

- [ ] No paste-related toggle on Overview tab; only in Settings → Privacy
- [ ] Sidebar project rows show ONLY status dot, name, folder path — NO relative time
- [ ] Home placeholder reads exactly: "Ready to check in? Type a sentence or two."
- [ ] Clicking a project card on Home updates the prompt and check-in target
- [ ] Prompt block is horizontally centered (max-width ~560pt) in the Home view
- [ ] "+ New Project" toolbar button is present and functional
- [ ] Visual verification done in the running app, not just code review

## Don't

- Don't ship this without launching the app and looking at every surface mentioned. The whole point of this prompt is that PR-12 was marked shipped without those visual checks.
- Don't add new features. This is purely re-land of PR-12.
- Don't change the rest of the home view (greeting style, project cards strip layout, "This week" footer). Just fix the items above.

## Touch

- `Sources/Views/Project/OverviewView.swift` — remove pasteboard toggle if still present
- `Sources/Views/Sidebar/SidebarView.swift` — remove relative-time line from project rows
- `Sources/Views/Home/HomeView.swift` — placeholder text fix; centered max-width frame; project-aware prompt binding
- `main.swift` or toolbar config — verify "+ New Project" button is present

When shipped, update this file's frontmatter `status: shipped` + `date_shipped`, and update `Procestra/Assets/Brand/PROMPT_LOG.md`. **Include in your shipped report a confirmation that you visually verified each of the six items above in a running build.**
